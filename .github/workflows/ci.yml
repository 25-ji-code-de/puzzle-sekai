name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Corepack must be enabled before setup-node's yarn cache lookup,
      # otherwise the system Yarn 1.x fails on packageManager: yarn@4.
      - name: Enable Corepack
        run: corepack enable
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: yarn
      - name: Install
        run: yarn install --immutable
      - name: Test
        run: yarn test
      - name: Typecheck
        run: yarn tsc --noEmit
      - name: Lint
        run: yarn lint
      - name: Format
        run: yarn format:check
      - name: Build
        run: yarn build
