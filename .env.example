# SEKAI Pass (public OAuth client — no secret in frontend)
VITE_SEKAI_PASS_ISSUER=https://id.nightcord.de5.net
VITE_SEKAI_PASS_CLIENT_ID=

# Gateway user sync
VITE_GATEWAY_BASE=https://api.nightcord.de5.net
VITE_SYNC_PROJECT=pico

# Native shells (Tauri desktop / Capacitor Android) — leave empty for web.
# Set VITE_NATIVE=1 via `.env.native` / `yarn build:native` only.
# VITE_NATIVE=
# VITE_NATIVE_REDIRECT_URI=puzzlesekai://auth/callback
