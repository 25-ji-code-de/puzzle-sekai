{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Default capability for the main window (deep-link OAuth return).",
  "windows": ["main"],
  "permissions": ["core:default", "deep-link:default"]
}
